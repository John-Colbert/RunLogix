using IO, System, Format;

class BuildWorkoutsClass {
  readonly var FileEntryClass[] StorageFiles;

  Constructor() {
    StorageFiles      = EnumerateFiles(DataSessionsPath);

    while (StorageFiles.Length > 0) {
      var i            = 0;
      var file         = StorageFiles[i];
      //var nextFile     = StorageFiles[i + 1];

      var fileDate     = GetDateStrFromFile(file);
      //var nextFileDate = GetDateStrFromFile(nextFile);

      //Check if last workout exists by fileDate

      //IF WORKOUT
      //  Confirm the file follows along appropriately with the workout
      //  IF SO
      //     Add to Workout
      //  ELSE
      //     Create new workout
      //ELSE
      //  Create new workout

      StorageFiles = StorageFiles.Delete(i);
    }
  }

  private GetDateStrFromFile(FileEntryClass file) {
    var filename = file.FileName;
    var fileNameParts = filename.Split("_");
    return fileNameParts[1];
  }
}

